<UserControl x:Class="WPFFuctionExample.IconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"           
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100" Loaded="UserControl_Loaded">
    <Grid Background="White">
        <TextBlock x:Name="Info"  FontSize="16" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <!--<Button >            
            <Button.Content>
                <StackPanel>
                    --><!--<Image x:Name="Icon" Width="100" Height="70" Stretch="Fill" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>--><!--
                    <TextBlock x:Name="Info" Text="{Binding BtnInfo}" FontSize="16" Height="30"/>
                </StackPanel>
            </Button.Content>
         </Button>-->       
    </Grid>
</UserControl>
